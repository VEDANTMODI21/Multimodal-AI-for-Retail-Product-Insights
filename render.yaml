services:
  - type: web
    name: Multimodal-AI-for-Retail-Product-Insights
    env: python
    branch: main
    buildCommand: python -m pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: gunicorn src.api:app
    plan: free
    autoDeploy: true
